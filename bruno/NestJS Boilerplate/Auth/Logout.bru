meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{host}}/auth/logout
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "email": "{{email}}",
    "password": "{{password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
